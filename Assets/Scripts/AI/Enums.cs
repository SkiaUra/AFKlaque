

public enum ItemType {
    WEAPON,
    HEAD,
    TRINKET,
    BEHAVIOUR
}

public enum StatType {
    HEALTH,
    ATTACK,
    RANGE
}

public enum ScreenType {
    EQUIPMENTSCREEN = 0,
    LOBBYSCREEN = 1
}